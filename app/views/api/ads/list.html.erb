<% this[:ad_bars].each do |ad_bar| %>
	<div class="advertising advertising-left" style="<%= ad_bar.css_style %>"> 
		<% ad_bar.ads.each do |ad| %>
			<div>
				<a href="<%= ad.url %>">
					<img src="<%= ad.image.url if ad.image.present? %>"> 
				</a>
			</div>
		<% end %>
	</div>
<% end %>